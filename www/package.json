{
  "name": "oops-game",
  "version": "1.0.3",
  "description": "Oops! - A deceptive platformer game",
  "main": "game.js",
  "scripts": {
    "build-apk": "npm install && npx cap init 'Oops!' com.chaosrealm.game --web-dir . && npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug",
    "cap-sync": "npx cap sync android",
    "cap-open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/cli": "^5.7.8",
    "@capacitor/core": "^5.7.8"
  }
}
